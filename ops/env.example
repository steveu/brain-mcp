# Example contents of ~/.config/brain-mcp/env — sourced by bin/brain-mcp-launcher.
# Create the real file with mode 600. Do NOT commit it.
#
#   mkdir -p ~/.config/brain-mcp
#   install -m 600 /dev/null ~/.config/brain-mcp/env
#   $EDITOR ~/.config/brain-mcp/env

# Bearer token Claude.ai sends in the Authorization header.
# Generate with: openssl rand -hex 32
BRAIN_MCP_TOKEN=

# Optional. Absolute path to the vault directory. Defaults to ~/brain/vault.
# BRAIN_VAULT=

# Optional. HTTP port (loopback bind). Defaults to 8765.
# PORT=
