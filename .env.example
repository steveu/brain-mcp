# Bearer token Claude.ai sends in the Authorization header.
# Generate with: openssl rand -hex 32
BRAIN_MCP_TOKEN=

# Absolute path to the vault directory.
# Defaults to ~/brain/vault if unset.
BRAIN_VAULT=

# HTTP port. Defaults to 8765.
PORT=
